<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Log Your Sleep Schedule</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f3f7f9;
            padding: 20px;
            color: #333;
        }
        .container {
            background-color: white;
            max-width: 600px;
            margin: auto;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0px 4px 10px rgba(0,0,0,0.1);
        }
        h2 {
            text-align: center;
            color: #2c3e50;
        }
        label {
            display: block;
            margin-top: 20px;
        }
        input[type="time"],
        input[type="date"],
        input[type="submit"],
        textarea {
            width: 100%;
            padding: 10px;
            margin-top: 5px;
            border-radius: 5px;
            border: 1px solid #ccc;
        }
        input[type="submit"] {
            background-color: #2ecc71;
            color: white;
            border: none;
            cursor: pointer;
            margin-top: 30px;
            font-weight: bold;
        }
        input[type="submit"]:hover {
            background-color: #27ae60;
        }
        .message {
            padding: 10px;
            margin-top: 20px;
            border-radius: 5px;
        }
        .success {
            background-color: #d4edda;
            color: #155724;
        }
        .error {
            background-color: #f8d7da;
            color: #721c24;
        }
    </style>
</head>
<body>
    <div class="container">
        <h2>Sleep Schedule Log</h2>
        <form method="POST">
            <label for="log_date">Date:</label>
            <input type="date" name="log_date" required>

            <label for="bedtime">Bedtime:</label>
            <input type="time" name="bedtime" required>

            <label for="wake_time">Wake Time:</label>
            <input type="time" name="wake_time" required>

            <label for="notes">Sleep Quality Notes (optional):</label>
            <textarea name="notes" rows="4" placeholder="e.g., Woke up feeling refreshed, had trouble falling asleep..."></textarea>

            <input type="submit" value="Log Sleep">
        </form>

        {% with messages = get_flashed_messages(with_categories=true) %}
          {% if messages %}
            {% for category, message in messages %}
              <div class="message {{ category }}">{{ message }}</div>
            {% endfor %}
          {% endif %}
        {% endwith %}
    </div>
</body>
</html>
